# 🔗 Интеграция с Битрикс24

## 📋 Оглавление
1. [Как это работает](#как-это-работает)
2. [Настройка Google Apps Script](#настройка-google-apps-script)
3. [Настройка Битрикс24](#настройка-битрикс24)
4. [Формат данных](#формат-данных)
5. [Тестирование](#тестирование)
6. [Поля в Битриксе](#поля-в-битриксе)

---

## 🔄 Как это работает

```
┌─────────────┐         POST запрос          ┌──────────────────┐
│  Битрикс24  │ ──────────────────────────>  │  Google Apps     │
│   (Сделка)  │   {client, address, phone}   │     Script       │
└─────────────┘                               └──────────────────┘
      ▲                                              │
      │                                              │ Создаёт:
      │                                              │ • Папку клиента
      │                                              │ • Смету (копия шаблона)
      │          Ответ с результатом                 │ • Выдаёт доступы
      │    {uid, smetaUrl, folderUrl}                │
      └──────────────────────────────────────────────┘
```

### Процесс:
1. **Битрикс** отправляет данные клиента через вебхук
2. **GAS** создаёт смету, папку, выдаёт доступы
3. **GAS** отправляет обратно в Битрикс: UID, ссылки на смету/папку
4. **Битрикс** обновляет поля сделки полученными данными

---

## ⚙️ Настройка Google Apps Script

### 1. Загрузка кода

Весь код уже в проекте:
- `7_WebhookBitrix.js` — обработчик вебхуков

### 2. Деплой веб-приложения

#### Через clasp (командная строка):
```bash
clasp push
clasp deploy --description "Bitrix24 Integration v1"
```

#### Через веб-интерфейс:
1. Открой проект: https://script.google.com/home
2. Найди проект по ID: `1EQiHV_ehdInMzIPRz_SKw63gvD0_XTwqHsGHCQdmsh6RSWjFU_fOwXwI`
3. Нажми **"Развернуть"** → **"Новое развертывание"**
4. Выбери тип: **"Веб-приложение"**
5. Настройки:
   - **Описание**: `Bitrix24 Webhook`
   - **Запуск от имени**: `Я`
   - **У кого есть доступ**: `Все`
6. Нажми **"Развернуть"**
7. **СКОПИРУЙ URL** — он понадобится для Битрикса

**Формат URL:**
```
https://script.google.com/macros/s/{DEPLOY_ID}/exec
```

---

## 🔧 Настройка Битрикс24

### 1. Создание пользовательских полей

Перейди: **CRM → Настройки → Пользовательские поля → Сделка**

Создай следующие поля:

| Название поля | Символьный код | Тип | Описание |
|--------------|----------------|-----|----------|
| UID сметы | `UF_CRM_SMETA_UID` | Строка | Уникальный ID объекта |
| Ссылка на смету | `UF_CRM_SMETA_LINK` | Ссылка | URL файла сметы |
| ID файла сметы | `UF_CRM_SMETA_ID` | Строка | Google Drive File ID |
| Папка клиента | `UF_CRM_FOLDER_LINK` | Ссылка | URL папки в Drive |

### 2. Настройка исходящего вебхука

#### A. Создание вебхука для отправки данных В GAS

1. Перейди: **Приложения → Вебхуки → Исходящий вебхук**
2. Нажми **"+ Добавить вебхук"**
3. Настройки:
   - **Название**: `Создание сметы в GAS`
   - **URL обработчика**: `{СКОПИРОВАННЫЙ URL ИЗ GAS}`
   - **Событие**: `При обновлении сделки` (ONCRMDEALUPDATE)
   - **Фильтр**: Стадия = "Делаем смету"
4. Сохрани

#### B. Настройка отправки данных

В настройках вебхука укажи **тело запроса (JSON)**:

```json
{
  "company": "{=Document:ASSIGNED_BY_ID.UF_DEPARTMENT}",
  "manager": "{=Document:ASSIGNED_BY_ID.NAME} {=Document:ASSIGNED_BY_ID.LAST_NAME}",
  "measurer": "{=Document:UF_CRM_MEASURER}",
  "client": "{=Document:TITLE}",
  "address": "{=Document:UF_CRM_ADDRESS}",
  "phone": "{=Document:PHONE[0].VALUE}",
  "managerId": "{=Document:ASSIGNED_BY_ID.ID}",
  "managerFolderId": "{=Document:ASSIGNED_BY_ID.UF_DRIVE_FOLDER}",
  "measurerEmail": "{=Document:UF_CRM_MEASURER_EMAIL}",
  "templateId": "{=Template:SMETA_TEMPLATE_ID}",
  "bitrixDealId": "{=Document:ID}",
  "bitrixWebhookUrl": "https://your-domain.bitrix24.ru/rest/1/{YOUR_WEBHOOK_TOKEN}/"
}
```

**ВАЖНО:** Замени:
- `your-domain` — на твой поддомен Битрикс24
- `{YOUR_WEBHOOK_TOKEN}` — на твой токен входящего вебхука (см. ниже)

#### C. Создание входящего вебхука (для получения ответа)

1. Перейди: **Приложения → Вебхуки → Входящий вебхук**
2. Нажми **"+ Добавить вебхук"**
3. Права доступа:
   - ✅ `crm` (CRM)
   - ✅ `crm.deal.update` (Изменение сделок)
4. Скопируй **URL вебхука** (формат: `https://your-domain.bitrix24.ru/rest/1/abc123xyz/`)
5. Этот URL укажи в поле `bitrixWebhookUrl` (см. выше)

---

## 📦 Формат данных

### Запрос ОТ Битрикса В GAS

```json
{
  "company": "БРЕНД-МАР",
  "manager": "Иванов Иван",
  "measurer": "Петров Петр",
  "client": "Сидоров Андрей",
  "address": "Москва, ул. Ленина, д. 10, кв. 5",
  "phone": "89991112233",
  "managerId": "IV123",
  "managerFolderId": "1ABC...XYZ",
  "measurerEmail": "measurer@example.com",
  "templateId": "1DEF...GHI",
  "bitrixDealId": "12345",
  "bitrixWebhookUrl": "https://your-domain.bitrix24.ru/rest/1/webhook_code/"
}
```

### Ответ ОТ GAS В Битрикс

```json
{
  "success": true,
  "data": {
    "uid": "IV123-42",
    "smetaUrl": "https://docs.google.com/spreadsheets/d/1ABC.../edit",
    "smetaId": "1ABC...XYZ",
    "folderUrl": "https://drive.google.com/drive/folders/1DEF...GHI",
    "action": "CREATE_NEW",
    "status": "Делаем смету",
    "message": "Смета успешно создана. UID: IV123-42"
  }
}
```

### Обновление сделки в Битриксе

GAS автоматически обновляет поля:
- `UF_CRM_SMETA_UID` = `"IV123-42"`
- `UF_CRM_SMETA_LINK` = `"https://docs.google.com/..."`
- `UF_CRM_SMETA_ID` = `"1ABC...XYZ"`
- `UF_CRM_FOLDER_LINK` = `"https://drive.google.com/..."`
- `COMMENTS` = `"Смета создана автоматически. UID: IV123-42"`

---

## 🧪 Тестирование

### Тест через Apps Script Editor

1. Открой файл `7_WebhookBitrix.js`
2. Найди функцию `testWebhook()`
3. Замени тестовые данные на реальные:
   - `managerFolderId` — ID папки в Google Drive
   - `templateId` — ID шаблона сметы
4. Запусти функцию через редактор
5. Проверь логи: `View → Logs` (Ctrl+Enter)

### Тест через Postman

**URL:** `{СКОПИРОВАННЫЙ URL ИЗ GAS}`

**Метод:** `POST`

**Headers:**
```
Content-Type: application/json
```

**Body:**
```json
{
  "company": "БРЕНД-МАР",
  "manager": "Иванов Иван",
  "measurer": "Петров Петр",
  "client": "Тестовый Клиент",
  "address": "Москва, ул. Тестовая, д. 1",
  "phone": "89991112233",
  "managerId": "TEST123",
  "managerFolderId": "1abc...xyz",
  "measurerEmail": "test@example.com",
  "templateId": "1def...ghi",
  "bitrixDealId": "12345",
  "bitrixWebhookUrl": "https://your-domain.bitrix24.ru/rest/1/webhook/"
}
```

**Ожидаемый ответ (200 OK):**
```json
{
  "success": true,
  "data": {
    "uid": "TEST123-1",
    "smetaUrl": "https://docs.google.com/...",
    "smetaId": "1ABC...",
    "folderUrl": "https://drive.google.com/...",
    "action": "CREATE_NEW",
    "status": "Делаем смету",
    "message": "Смета успешно создана. UID: TEST123-1"
  }
}
```

### Проверка в Битриксе

1. Открой сделку
2. Проверь, что поля обновились:
   - ✅ **UID сметы**: заполнено
   - ✅ **Ссылка на смету**: кликабельна
   - ✅ **Папка клиента**: открывается
3. Перейди по ссылке на смету — должен открыться файл

---

## 📝 Поля в Битриксе

### Обязательные для ОТПРАВКИ в GAS:

| Поле Битрикса | Символьный код | Пример |
|--------------|----------------|--------|
| Название сделки | `TITLE` | "Сидоров Андрей" |
| Адрес | `UF_CRM_ADDRESS` | "Москва, ул. Ленина, д. 10" |
| Телефон | `PHONE[0].VALUE` | "89991112233" |
| Ответственный (ID) | `ASSIGNED_BY_ID.ID` | "123" |
| Замерщик | `UF_CRM_MEASURER` | "Петров Петр" |
| Email замерщика | `UF_CRM_MEASURER_EMAIL` | "measurer@example.com" |
| Юрлицо | `ASSIGNED_BY_ID.UF_DEPARTMENT` | "БРЕНД-МАР" |

### Обновляются АВТОМАТИЧЕСКИ после создания сметы:

| Поле | Символьный код | Заполняется |
|------|----------------|-------------|
| UID сметы | `UF_CRM_SMETA_UID` | Автоматически |
| Ссылка на смету | `UF_CRM_SMETA_LINK` | Автоматически |
| ID файла сметы | `UF_CRM_SMETA_ID` | Автоматически |
| Папка клиента | `UF_CRM_FOLDER_LINK` | Автоматически |

---

## 🔐 Безопасность

### Рекомендации:

1. **Ограничь доступ к вебхукам** — используй IP-фильтры
2. **Проверяй токены** — добавь аутентификацию в `doPost()`
3. **Логируй запросы** — для отладки и мониторинга
4. **Ограничь права** — вебхук должен обновлять только сделки

### Пример добавления токена:

```javascript
function doPost(e) {
  const SECRET_TOKEN = 'your_secret_token_here';
  
  try {
    const payload = JSON.parse(e.postData.contents);
    
    // Проверка токена
    if (payload.token !== SECRET_TOKEN) {
      throw new Error('Неверный токен доступа');
    }
    
    // ... остальной код
  } catch (error) {
    // ...
  }
}
```

---

## 🐛 Отладка

### Логи в Apps Script:
```javascript
Logger.log('Проверка данных: ' + JSON.stringify(payload));
```

Просмотр: **View → Logs** (Ctrl+Enter)

### Логи в Битриксе:
**Приложения → Вебхуки → История вызовов**

Здесь видны:
- ✅ Отправленные запросы
- ✅ Полученные ответы
- ❌ Ошибки

### Частые проблемы:

| Проблема | Причина | Решение |
|----------|---------|---------|
| 401 Unauthorized | Неверный URL деплоя | Пересоздай развертывание |
| 500 Internal Error | Ошибка в коде GAS | Проверь логи Apps Script |
| Битрикс не обновляется | Неверный токен вебхука | Проверь `bitrixWebhookUrl` |
| Нет доступа к смете | Неверный email | Проверь `measurerEmail` |

---

## 📞 Поддержка

При проблемах проверь:
1. Логи Apps Script: [script.google.com](https://script.google.com)
2. Историю вебхуков в Битриксе
3. Права доступа к папкам/файлам в Google Drive

**ID проекта GAS:** `1EQiHV_ehdInMzIPRz_SKw63gvD0_XTwqHsGHCQdmsh6RSWjFU_fOwXwI`
