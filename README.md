# 📊 Генератор смет и документов

Google Apps Script проект для автоматизации создания смет, договоров и счетов.

## 🚀 Возможности

- ✅ **Создание смет** с автоматической дедупликацией клиентов
- 📜 **Генерация договоров** из шаблонов
- 💰 **Выставление счетов** через T-Bank API
- 🔄 **Синхронизация** с транзитной таблицей
- 📁 **Управление папками** в Google Drive
- 🔗 **Интеграция с Битрикс24** через вебхуки

## 📦 Структура проекта

```
/
├── 1_Config.js           # Конфигурация и константы
├── 2_Helpers.js          # Вспомогательные функции
├── 3_Deduplacation.js    # Система дедупликации (алгоритм Левенштейна)
├── 4_CreateEstimate.js   # Создание смет
├── 5_CreateContracts.js  # Генерация договоров
├── 6_Invoices.js         # Работа со счетами
├── 7_WebhookBitrix.js    # Интеграция с Битрикс24
├── appsscript.json       # Манифест проекта
├── .clasp.json           # Конфигурация Clasp
└── BITRIX_INTEGRATION.md # Инструкция по интеграции
```

## 🔧 Установка

### 1. Клонирование проекта

```bash
# Установка Clasp (если ещё не установлен)
npm install -g @google/clasp

# Авторизация (если ещё не авторизован)
clasp login

# Клонирование проекта
clasp clone 1EQiHV_ehdInMzIPRz_SKw63gvD0_XTwqHsGHCQdmsh6RSWjFU_fOwXwI
```

### 2. Загрузка изменений

```bash
# Отправка изменений в GAS
clasp push

# Принудительная отправка (при изменении манифеста)
clasp push --force
```

### 3. Деплой веб-приложения (для Битрикс24)

```bash
# Создание развертывания
clasp deploy --description "Production v1"

# Просмотр развертываний
clasp deployments
```

## 🔗 Интеграция с Битрикс24

Подробная инструкция: [BITRIX_INTEGRATION.md](BITRIX_INTEGRATION.md)

### Быстрый старт:

1. **Деплой веб-приложения** в Apps Script
2. **Создание пользовательских полей** в Битриксе:
   - `UF_CRM_SMETA_UID` — UID сметы
   - `UF_CRM_SMETA_LINK` — Ссылка на смету
   - `UF_CRM_FOLDER_LINK` — Папка клиента
3. **Настройка исходящего вебхука** в Битриксе
4. **Тестирование** через Postman или встроенные функции

## 📚 Документация

### Основные функции

#### `createEstimate()`
Создаёт смету для клиента:
- Проверяет дубликаты по адресу (алгоритм Левенштейна)
- Создаёт папку клиента в Google Drive
- Копирует шаблон сметы
- Выдаёт доступы замерщику
- Синхронизирует с транзитной таблицей

#### `createEstimateFromBitrix(data)`
Аналог `createEstimate()`, но для вебхуков:
- Принимает данные через JSON
- Возвращает результат для Битрикса
- Автоматически обновляет поля сделки

#### `doPost(e)`
Обработчик входящих вебхуков от Битрикс24:
- Парсит JSON из запроса
- Валидирует обязательные поля
- Вызывает `createEstimateFromBitrix()`
- Отправляет результат обратно

### Константы колонок (COL)

```javascript
COL.A_UID: 1          // A — UID объекта
COL.A_CLIENT: 2       // B — Имя клиента
COL.B_PHONE: 3        // C — Телефон
COL.C_ADDR: 4         // D — Адрес
COL.D_STATUS: 5       // E — Статус
COL.K_SMETA_LINK: 12  // L — Ссылка на смету
COL.O_FOLDER: 16      // P — Папка клиента
```

## 🧪 Тестирование

### Тест создания сметы через Битрикс

```javascript
function testWebhook() {
  const testData = {
    company: 'БРЕНД-МАР',
    manager: 'Иванов Иван',
    measurer: 'Петров Петр',
    client: 'Тестовый Клиент',
    address: 'Москва, ул. Тестовая, д. 1',
    phone: '89991112233',
    managerId: 'TEST123',
    managerFolderId: '1abc...xyz',
    measurerEmail: 'test@example.com',
    templateId: '1def...ghi'
  };
  
  const result = createEstimateFromBitrix(testData);
  Logger.log(JSON.stringify(result, null, 2));
}
```

### Тест через Postman

**POST** `https://script.google.com/macros/s/{DEPLOY_ID}/exec`

**Body (JSON):**
```json
{
  "company": "БРЕНД-МАР",
  "client": "Тестовый Клиент",
  "address": "Москва, ул. Ленина, д. 1",
  "phone": "89991112233",
  ...
}
```

## 🔐 Настройки

### Конфигурация (1_Config.js)

```javascript
const TRANSIT_FILE_ID = '1u1VqcokQxDQ9MtuMYwPH_1FBrKXeIM4t95QwDs16M1k';
const ALWAYS_EDITOR   = 'ikris.0077@gmail.com';
const API_URL_CREATE  = 'https://business.tbank.ru/openapi/api/v1/invoice/send';
```

### Названия листов

```javascript
const SHEET_EST_INPUT      = '✅ СМЕТА';
const SHEET_TECH           = 'Тех';
const OBJECTS_SHEET_NAME   = 'ОБЪЕКТЫ';
const REGISTRY_SHEET_NAME  = 'РЕЕСТР СЧЕТОВ';
```

## 📊 Логирование

Просмотр логов:
1. Открой [script.google.com](https://script.google.com)
2. Выбери проект
3. **View → Logs** (или Ctrl+Enter)

Добавление логов в код:
```javascript
Logger.log('Сообщение: ' + JSON.stringify(data));
```

## 🐛 Отладка

### Частые проблемы

| Проблема | Причина | Решение |
|----------|---------|---------|
| Блокировка не получена | Одновременное выполнение | Подожди 30 сек |
| Телефон не нормализуется | Неверный формат | Используй +79991112233 |
| Дубликат не найден | Порог сходства <85% | Проверь адрес |
| Нет доступа к файлу | Неверный email | Проверь `measurerEmail` |

### Логи в Битриксе

**Приложения → Вебхуки → История вызовов**

## 📞 Поддержка

**Проект ID:** `1EQiHV_ehdInMzIPRz_SKw63gvD0_XTwqHsGHCQdmsh6RSWjFU_fOwXwI`

**Ссылка на проект:** https://script.google.com/home/projects/1EQiHV_ehdInMzIPRz_SKw63gvD0_XTwqHsGHCQdmsh6RSWjFU_fOwXwI

---

## 📝 Changelog

### v2.0 (2025-12-25)
- ✅ Добавлена интеграция с Битрикс24
- ✅ Веб-приложение для приёма вебхуков
- ✅ Автоматическая отправка результата в Битрикс
- ✅ Подробная документация

### v1.0
- ✅ Создание смет
- ✅ Дедупликация клиентов
- ✅ Генерация договоров
- ✅ Выставление счетов
